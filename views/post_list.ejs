<!DOCTYPE html>
<html>

<head>
    <title>게시판</title>
    <link rel="stylesheet" href="/style.css">

</head>

<body>
    <div class="container">
        <h1>📝 자유 게시판 (XSS 취약)</h1>
        <p>로그인 사용자: <b><%= user.username %></b> | <a href="/auth/logout">로그아웃</a></p>
        <a href="/post/write"><button>새 글 쓰기</button></a>
        <hr>

        <% posts.forEach(post=> { %>
            <div class="post-box">
                <h3>제목: <%= post.title %>
                </h3>
                <!-- <%- %> 는 HTML 태그를 해석해서 실행해버림 -->
                <p>내용: <%- post.content %></p>
                <small>작성자 ID: <%= post.user_id %> | 시간: <%= post.created_at %></small>
                <br>
                <a href="/post/edit/<%= post.id %>"><button>수정</button></a>
            </div>
            <% }) %>

            <a href="/">홈으로</a>
    </div>
</body>

</html>